% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_consensus_pipeline}
\alias{run_consensus_pipeline}
\title{Run the full QC + consensus doublet detection pipeline}
\usage{
run_consensus_pipeline(
  seurat_obj,
  run_qc = TRUE,
  qc_args = list(),
  mt_pattern = "^MT-",
  PCs = 1:10,
  expected_doublet_rate = 0.075,
  consensus_method = c("rank", "weighted"),
  weights = c(pANN = 0.4, scDblFinder_score = 0.6),
  top_n = NULL,
  quantile = 0.9,
  cutoff_method = c("legacy", "intersection_median", "multiplet_rate"),
  multiplet_rate = NULL,
  seed = 1
)
}
\arguments{
\item{seurat_obj}{A \code{Seurat} object with raw counts, already normalized
and dimensionality-reduced (\code{RunPCA()} already run) if \code{run_qc = FALSE}.}

\item{run_qc}{Whether to compute QC metrics and filter cells first.}

\item{qc_args}{Named list of arguments passed to \code{\link[=filter_cells]{filter_cells()}}.}

\item{mt_pattern}{Passed to \code{\link[=compute_qc_metrics]{compute_qc_metrics()}}.}

\item{PCs}{Principal components passed to \code{\link[=run_doubletfinder]{run_doubletfinder()}}, or
\code{"auto"} to pick them via \code{\link[=choose_pcs]{choose_pcs()}}.}

\item{expected_doublet_rate}{Passed to \code{\link[=run_doubletfinder]{run_doubletfinder()}}, or \code{"auto"}
to derive it from cell count via \code{\link[=estimate_multiplet_rate]{estimate_multiplet_rate()}} -- run
automatically at the start of every call (see the reported estimate in
the printed message from \code{\link[=inspect_input]{inspect_input()}}).}

\item{consensus_method}{\code{"rank"} or \code{"weighted"}, passed to
\code{\link[=consensus_rank]{consensus_rank()}}.}

\item{weights}{Optional named weights for \code{pANN}/\code{scDblFinder_score},
passed to \code{\link[=consensus_rank]{consensus_rank()}}. Defaults to \code{c(pANN = 0.4, scDblFinder_score = 0.6)}: scDblFinder is weighted higher based on
published benchmarks (e.g. Xi & Li 2021) generally finding it more
accurate and less parameter-sensitive than DoubletFinder. This is a
literature-informed prior, not a dataset-specific optimum -- pass your
own \code{weights}, or derive one from your data with \code{\link[=optimize_weights]{optimize_weights()}}.}

\item{top_n, quantile}{Passed to \code{\link[=classify_consensus_doublets]{classify_consensus_doublets()}} when
\code{cutoff_method = "legacy"}.}

\item{cutoff_method, multiplet_rate}{Passed to
\code{\link[=classify_consensus_doublets]{classify_consensus_doublets()}}. \code{call_cols} for
\code{cutoff_method = "intersection_median"} is fixed to
\code{c("doublet_finder_call", "scdblfinder_call")}.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A list with \code{seurat_obj} (post-QC), \code{consensus_df} (per-cell
scores and calls), and \code{comparison} (output of
\code{\link[=compare_to_intersection]{compare_to_intersection()}}).
}
\description{
Convenience wrapper: QC filtering, DoubletFinder, scDblFinder, and
consensus scoring in one call. For finer control (e.g. inspecting QC
plots before choosing thresholds) call the individual functions directly.
}
