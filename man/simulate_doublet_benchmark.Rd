% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{simulate_doublet_benchmark}
\alias{simulate_doublet_benchmark}
\title{Simulate a ground-truth doublet benchmark dataset}
\usage{
simulate_doublet_benchmark(n_cells = 3000, doublet_rate = 0.1, seed = 1, ...)
}
\arguments{
\item{n_cells}{Number of cells to simulate.}

\item{doublet_rate}{True doublet rate to simulate.}

\item{seed}{Random seed for reproducibility.}

\item{...}{Additional arguments passed to \code{\link[=run_consensus_pipeline]{run_consensus_pipeline()}}.}
}
\value{
A list with \code{consensus_df} (as returned by
\code{\link[=run_consensus_pipeline]{run_consensus_pipeline()}}, with an added \code{truth} column: \code{"doublet"}/
\code{"singlet"}) and \code{truth} (the same labels as a standalone vector, in
\code{consensus_df$cell} order).
}
\description{
No ground-truth doublet labels exist for real data without cell-hashing or
genotype demultiplexing, so accuracy can't be measured directly on a
typical dataset. This generates a synthetic dataset with known doublet
labels (via \code{scDblFinder::mockDoubletSCE()}, which builds doublets by
summing pairs of real single-cell profiles -- the standard approach used
in doublet-detection benchmark papers) and runs the full consensus
pipeline on it.
}
