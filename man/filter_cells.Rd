% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{filter_cells}
\alias{filter_cells}
\title{Filter low-quality cells}
\usage{
filter_cells(
  seurat_obj,
  min_genes = 200,
  max_genes = Inf,
  min_counts = 500,
  max_mt_percent = 10
)
}
\arguments{
\item{seurat_obj}{A \code{Seurat} object with QC metrics computed via
\code{\link[=compute_qc_metrics]{compute_qc_metrics()}}.}

\item{min_genes}{Minimum \code{nFeature_RNA} to keep a cell.}

\item{max_genes}{Maximum \code{nFeature_RNA} to keep a cell (guards against
multiplets that slipped past doublet detection).}

\item{min_counts}{Minimum \code{nCount_RNA} to keep a cell.}

\item{max_mt_percent}{Maximum \code{percent.mt} to keep a cell.}
}
\value{
The subset \code{Seurat} object.
}
\description{
Subsets a Seurat object on gene count per cell, total RNA counts, and
mitochondrial percentage. Thresholds must be chosen per-dataset (see
\code{\link[=plot_qc_metrics]{plot_qc_metrics()}}) -- there is no universally correct cutoff.
}
