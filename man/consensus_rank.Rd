% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus.R
\name{consensus_rank}
\alias{consensus_rank}
\title{Build a rank- or weight-based doublet consensus}
\usage{
consensus_rank(
  score_df,
  cell_col = "cell",
  weights = NULL,
  method = c("rank", "weighted"),
  normalize_method = c("zscore", "minmax")
)
}
\arguments{
\item{score_df}{A \code{data.frame} with one row per cell: an identifier column
(\code{cell_col}) plus one numeric column of raw scores per detection tool
(e.g. \code{pANN}, \code{scDblFinder_score}).}

\item{cell_col}{Name of the cell identifier column.}

\item{weights}{Optional named numeric vector giving a weight per score
column (names must match \code{score_df} column names). Defaults to equal
weighting. Only used when \code{method = "weighted"}.}

\item{method}{\code{"rank"} averages each tool's per-cell rank (weighted);
\code{"weighted"} sums normalized scores directly (weighted).}

\item{normalize_method}{Passed to \code{\link[=normalize_scores]{normalize_scores()}}.}
}
\value{
\code{score_df} with added \verb{<tool>_norm} columns (and \verb{<tool>_rank}
columns when \code{method = "rank"}), plus \code{consensus_score} and
\code{consensus_rank} (1 = most doublet-like), sorted by \code{consensus_rank}.
}
\description{
This is the core innovation of the package: rather than intersecting
per-tool doublet calls, normalize each tool's score and combine them into
a single consensus score/rank. This recovers cells that individual tools
scored with high confidence but that fall outside the strict intersection
of binary calls.
}
