% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{optimize_weights}
\alias{optimize_weights}
\title{Grid-search the DoubletFinder/scDblFinder weight ratio against ground truth}
\usage{
optimize_weights(
  score_df,
  truth,
  weight_grid = seq(0, 1, by = 0.05),
  method = c("rank", "weighted"),
  normalize_method = c("zscore", "minmax"),
  multiplet_rate = NULL,
  cell_col = "cell"
)
}
\arguments{
\item{score_df}{A \code{data.frame} with columns \code{cell}, \code{pANN},
\code{scDblFinder_score} (e.g. from merging \code{\link[=run_doubletfinder]{run_doubletfinder()}} and
\code{\link[=run_scdblfinder]{run_scdblfinder()}} output).}

\item{truth}{Named or row-aligned ground-truth vector, as in
\code{\link[=evaluate_against_truth]{evaluate_against_truth()}}.}

\item{weight_grid}{Numeric vector in \verb{[0, 1]}, each value the weight given
to \code{scDblFinder_score} (with \code{1 - value} given to \code{pANN}).}

\item{method}{Passed to \code{\link[=consensus_rank]{consensus_rank()}}.}

\item{normalize_method}{Passed to \code{\link[=consensus_rank]{consensus_rank()}}.}

\item{multiplet_rate}{Doublet rate used for the top-N cutoff at each grid
point (via \code{cutoff_method = "multiplet_rate"} in
\code{\link[=classify_consensus_doublets]{classify_consensus_doublets()}}). Defaults to the true rate implied by
\code{truth}.}

\item{cell_col}{Name of the cell identifier column.}
}
\value{
A list with \code{grid} (a \code{data.frame} of every weight split and its
resulting F1/sensitivity/precision) and \code{best_weights} (the named weight
vector with the highest F1).
}
\description{
The package's default weighting between DoubletFinder's \code{pANN} and
scDblFinder's score is a literature-informed prior (see
\code{?run_consensus_pipeline}), not a dataset-specific optimum. When a labeled
benchmark is available (synthetic via \code{\link[=simulate_doublet_benchmark]{simulate_doublet_benchmark()}}, or
real ground truth), this searches a grid of weight splits and reports the
one that maximizes F1.
}
