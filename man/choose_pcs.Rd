% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input_utils.R
\name{choose_pcs}
\alias{choose_pcs}
\title{Choose principal components via an elbow heuristic}
\usage{
choose_pcs(x, cumulative_cutoff = 90, variation_cutoff = 5, reduction = "pca")
}
\arguments{
\item{x}{Either a \code{Seurat} object with \code{RunPCA()} already run, or a numeric
vector of per-PC standard deviations (\code{Seurat::Stdev(seurat_obj, "pca")}).}

\item{cumulative_cutoff}{Minimum cumulative percent variance explained.}

\item{variation_cutoff}{Maximum percent variance explained by a single PC
at the candidate cutoff.}

\item{reduction}{Reduction name to read \code{Stdev()} from when \code{x} is a
\code{Seurat} object.}
}
\value{
An integer vector \code{1:n} giving the chosen PCs.
}
\description{
Picks the smallest number of leading PCs such that cumulative variance
explained exceeds \code{cumulative_cutoff}\% while the individual PC still
explains at least \code{variation_cutoff}\% -- falling back to the point where
variance explained by consecutive PCs differs by less than 0.1 percentage
points. This mirrors the elbow-plot heuristic commonly used with Seurat's
\code{ElbowPlot()}, automated so \code{PCs = "auto"} can be passed to
\code{\link[=run_consensus_pipeline]{run_consensus_pipeline()}} instead of eyeballing a plot.
}
