% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{evaluate_against_truth}
\alias{evaluate_against_truth}
\title{Evaluate doublet calls against ground-truth labels}
\usage{
evaluate_against_truth(consensus_df, truth, call_cols, cell_col = "cell")
}
\arguments{
\item{consensus_df}{Output of \code{\link[=classify_consensus_doublets]{classify_consensus_doublets()}}, merged with
the per-tool call columns named in \code{call_cols}.}

\item{truth}{Named character/logical vector of ground-truth labels
(\code{"doublet"}/\code{"singlet"} or \code{TRUE}/\code{FALSE}), named by \code{cell_col} values,
or an unnamed vector aligned row-for-row with \code{consensus_df}.}

\item{call_cols}{Character vector of per-tool call columns to intersect
for the comparison baseline.}

\item{cell_col}{Name of the cell identifier column.}
}
\value{
A \code{data.frame} with one row per method (\code{"consensus"},
\code{"intersection"}) and columns \code{n_called}, \code{sensitivity}, \code{precision},
\code{specificity}, \code{f1}.
}
\description{
Compares \code{\link[=run_consensus_pipeline]{run_consensus_pipeline()}}'s \code{consensus_call} against the strict
intersection of \code{call_cols}, scoring both against known-correct labels.
Works with any \code{truth} vector -- synthetic (from
\code{\link[=simulate_doublet_benchmark]{simulate_doublet_benchmark()}}) or real (e.g. cell-hashing/genotype
demultiplexing calls), so it doubles as the entry point for benchmarking
on a labeled dataset when one becomes available.
}
