% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_consensus_overview}
\alias{plot_consensus_overview}
\title{Plot where intersection-called cells fall in the consensus score distribution}
\usage{
plot_consensus_overview(
  consensus_df,
  call_cols,
  show_intersection_median_cutoff = TRUE,
  show_multiplet_rate_cutoff = TRUE,
  multiplet_rate = NULL
)
}
\arguments{
\item{consensus_df}{Output of \code{\link[=classify_consensus_doublets]{classify_consensus_doublets()}}, merged with
the per-tool call columns named in \code{call_cols}.}

\item{call_cols}{Character vector of per-tool call columns to intersect
for the color aesthetic (e.g. \code{c("doublet_finder_call", "scdblfinder_call")}).}

\item{show_intersection_median_cutoff}{Draw a horizontal line at the
intersection-median cutoff (see \code{\link[=classify_consensus_doublets]{classify_consensus_doublets()}}).}

\item{show_multiplet_rate_cutoff}{Draw a vertical line at the multiplet-
rate top-N cutoff (see \code{\link[=classify_consensus_doublets]{classify_consensus_doublets()}}).}

\item{multiplet_rate}{Passed to \code{\link[=estimate_multiplet_rate]{estimate_multiplet_rate()}}'s override when
drawing the multiplet-rate cutoff; defaults to the automatic estimate.}
}
\value{
A \code{ggplot} object.
}
\description{
Answers the question "where do the strict-intersection doublets sit within
the full consensus score distribution?" -- every cell is plotted by
\code{consensus_rank} (x) vs. \code{consensus_score} (y), colored by whether it was
called a doublet by every tool in \code{call_cols} (the intersection), with
shape marking the final \code{consensus_call}. Optional cutoff lines from
\code{\link[=classify_consensus_doublets]{classify_consensus_doublets()}}'s \code{"intersection_median"} and
\code{"multiplet_rate"} methods are overlaid for comparison.
}
