% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus.R
\name{classify_consensus_doublets}
\alias{classify_consensus_doublets}
\title{Classify cells as doublets from a consensus score}
\usage{
classify_consensus_doublets(
  consensus_df,
  top_n = NULL,
  quantile = 0.9,
  cutoff_method = c("legacy", "intersection_median", "multiplet_rate"),
  call_cols = NULL,
  multiplet_rate = NULL
)
}
\arguments{
\item{consensus_df}{Output of \code{\link[=consensus_rank]{consensus_rank()}}.}

\item{top_n}{If set, the \code{top_n} cells by \code{consensus_rank} are called
doublets. Takes precedence over \code{quantile}. Only used when
\code{cutoff_method = "legacy"}.}

\item{quantile}{Quantile of \code{consensus_score} (0-1) above which a cell is
called a doublet. Ignored if \code{top_n} is set. Default \code{0.9}. Only used
when \code{cutoff_method = "legacy"}.}

\item{cutoff_method}{\code{"legacy"} (default) uses \code{top_n}/\code{quantile} as
above. \code{"intersection_median"} sets the threshold to the median
\code{consensus_score} among cells called doublets by every column in
\code{call_cols} -- i.e. cells at least as doublet-like as a "typical"
intersection hit are kept, recovering ones the strict intersection
missed. \code{"multiplet_rate"} calls the top
\code{round(multiplet_rate * nrow(consensus_df))} cells by \code{consensus_rank},
where \code{multiplet_rate} defaults to \code{\link[=estimate_multiplet_rate]{estimate_multiplet_rate()}} applied
to the cell count.}

\item{call_cols}{Character vector of per-tool call columns (e.g.
\code{c("doublet_finder_call", "scdblfinder_call")}). Required when
\code{cutoff_method = "intersection_median"}.}

\item{multiplet_rate}{Expected doublet rate in \verb{[0, 1]}. Required only to
override the automatic estimate when \code{cutoff_method = "multiplet_rate"}.}
}
\value{
\code{consensus_df} with an added \code{consensus_call} column
(\code{"doublet"}/\code{"singlet"}).
}
\description{
Classify cells as doublets from a consensus score
}
