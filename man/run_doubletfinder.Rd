% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doubletfinder_wrapper.R
\name{run_doubletfinder}
\alias{run_doubletfinder}
\title{Run DoubletFinder and extract a per-cell doublet score}
\usage{
run_doubletfinder(
  seurat_obj,
  PCs = 1:10,
  expected_doublet_rate = 0.075,
  sct = FALSE,
  seed = 1
)
}
\arguments{
\item{seurat_obj}{A processed \code{Seurat} object.}

\item{PCs}{Integer vector of principal components to use, or \code{"auto"} to
pick them via \code{\link[=choose_pcs]{choose_pcs()}}'s elbow heuristic (requires \code{RunPCA()} to
have already been run on \code{seurat_obj}).}

\item{expected_doublet_rate}{Expected doublet rate (e.g. \code{0.075} for the
standard 10x ~7.5\% at ~10k cells loaded), or \code{"auto"} to derive it from
cell count via \code{\link[=estimate_multiplet_rate]{estimate_multiplet_rate()}}.}

\item{sct}{Whether the object was normalized with \code{SCTransform()}.}

\item{seed}{Random seed passed through for reproducibility.}
}
\value{
A \code{data.frame} with columns \code{cell}, \code{pANN} (raw DoubletFinder
score), and \code{doublet_finder_call} (\code{"Doublet"}/\code{"Singlet"}).
}
\description{
Thin wrapper around the \code{DoubletFinder} package (synthetic-doublet
nearest-neighbor comparison). Requires a normalized, dimensionality-reduced
Seurat object (\code{NormalizeData()}, \code{FindVariableFeatures()}, \code{ScaleData()},
\code{RunPCA()} already run).
}
